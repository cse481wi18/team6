recycle/

	launch/			(launch files)

		recyclo_bot.launch
			* the final launch file for the entire RecycloBot program

		controller.launch
			* launches controller related nodes/packages
			* map_server, amcl, move_base, move_group, controller

		classifier.launch
			* launches classifier related nodes/packages
			
	scripts/		(execution scripts)



	src/recycle/	(object/library files)

		controller.py
			* the Controller object
